# 网页内容边界修复 TODO

本文记录 Hacker News 与 GitHub 翻译异常的根因、设计边界、实施任务和验收标准。已落地并通过完整检查的项目会勾选，未勾选项目仍是后续工作。

## TODO 0（P0）：跳过持续变化的动态文本

产品边界：翻译新加载且稳定的正文，不翻译轮播、计数器、状态模拟和逐字动画等持续变化的文本。不能通过给所有动态内容增加数秒等待来规避问题；无限滚动文章、SPA 新页面和懒加载正文仍应及时翻译。

- [ ] 扫描时直接排除 `aria-hidden="true"`、`inert` 等语义隐藏或纯装饰子树，避免装饰性重复文本进入请求和进度统计。
- [ ] 识别同一内容单元短时间内的连续文本变化；确认其为易变内容后，取消待处理翻译并在本次运行中跳过。
- [ ] 识别同一容器反复创建、替换或回收 fresh DOM 节点的情况，避免相同轮播或状态记录按新 `Element` 身份无限累计。
- [ ] 易变内容和语义隐藏内容不创建翻译请求、不写入 `data-bt-source`，也不计入 `total` 或 `completed`。
- [ ] `class`、`style` 和纯布局动画不能持续占用 pending 状态；完成判定只等待真实的扫描、翻译和渲染工作。
- [ ] 保留现有正常动态内容能力：只新增一次并保持稳定的文章、评论、SPA 正文和懒加载内容仍进入翻译流程。
- [ ] 将通用站点的 `href` 变化重扫收窄到确实需要它的 GitHub 页面边界，避免无关 hydration 放大扫描。
- [ ] 使用 Cloudflare 首页结构制作最小静态 fixture，覆盖 Agents 文本扰动、Workflow 状态更新、客户故事轮播和 `aria-hidden` 装饰文本。
- [ ] 增加“持续变化内容计数有界”和“纯样式动画仍能完成”的回归测试，同时保留稳定动态正文测试。

验收标准：

- Cloudflare fixture 持续产生动画和轮播变化后，翻译运行能够进入完成状态，完成数不会随时间无限增长。
- 易变文本不会反复调用翻译服务；停止动画不要求补翻历史中间帧。
- 普通新增正文继续沿用现有短防抖，不为所有候选增加统一的长时间稳定等待。
- 单次扫描与增量翻译都遵守同一内容边界，关闭增量翻译不是修复成立的前提。
- 静态检查、单元测试和集成测试全部通过；除非单独授权，不使用浏览器自动化或视觉 QA。

## 目标

- Hacker News 的积分、作者、时间、隐藏入口和评论数不再产生残缺译文。
- GitHub contribution calendar 等固定布局可视组件不再被译文节点破坏。
- GitHub 仓库语言占比中的编程语言、标记语言、样式语言和框架名称不提交翻译，也不显示重复译文。
- `owner/repository` 等嵌在句子中的技术标识保持原样，只翻译句子的自然语言部分。
- 所有站点特例必须按主机名收口，不能用通用类名或宽泛文本正则影响其他网站。
- 使用静态 DOM、单元测试、集成测试和构建检查验证，不依赖浏览器自动化或视觉 QA。

## 已确认根因

### 1. Hacker News 元信息被拆成残片

扫描器会把链接识别为独立内容单元。Hacker News 的作者、时间、`hide` 和评论数都是链接，非链接文本则落到外层结构候选中，最终形成类似：

```text
35 points by | |
```

当前元信息识别主要依赖 `<time>`、`rel="author"`、`itemprop` 等语义标记。Hacker News 的元信息结构缺少这些标记，因此残片被当成正文提交给翻译服务。

### 2. GitHub contribution calendar 被当成普通表格

贡献图的月份、星期、图例和统计标签依赖固定单元格尺寸、坐标或紧凑布局。当前渲染器会在 `td`、`th` 等元素中追加块级译文节点，导致标签换行、重叠并改变网格尺寸。

这类组件不是普通数据表格。即使翻译文本本身正确，向组件内部插入额外 DOM 也会破坏展示。

### 3. 技术标识只在“整个候选”层面识别

当前过滤器能跳过独立的 `yuukiLike/cc-md-vault`，但下面的完整句子不会被识别为技术标识：

```text
Contributed to yuukiLike/cc-md-vault, yuukiLike/music-eyes,
yuukiLike/cc-md-preview and 107 other repositories
```

整句发送给模型后，仓库名可能被翻译、遗漏、改写或重新断行。只增强提示词不能保证标识符逐字节保持不变。

### 4. GitHub 语言占比标签不是自然语言正文

`JavaScript 88.9%`、`CSS 6.3%`、`Vue 4.3%`、`HTML 0.5%` 属于仓库技术栈统计。即使模型原样返回技术名称，渲染器仍可能插入一份相同译文，造成 `JavaScript / JavaScript` 之类的重复展示。

不能通过全局语言名称黑名单解决这个问题。GitHub 的统计项还包含标记语言、样式语言、框架和模板语言；`Go`、`R`、`C`、`Hack`、`Processing`、`Crystal` 等名称在普通正文中也有其他含义。脱离组件上下文进行全局匹配会产生误伤。

## 设计原则

### 站点规则只描述页面边界

新增一个集中、可测试的站点策略层，按精确主机名返回规则。站点规则只负责两件事：

- 标记纯元信息容器。
- 排除不能安全插入译文的复杂可视组件。

禁止把 `points by`、`.subtext`、`.score` 等规则直接加入全局过滤器。相同文本或类名可能出现在完全无关的网站中。

### 内容语义与页面布局分开处理

- “这是元信息吗”由候选分类和用户的内容过滤设置决定。
- “插入译文会破坏组件吗”属于安全边界，应无条件排除。
- “这是技术栈统计吗”由组件上下文判断，不通过枚举所有语言名称判断。
- “句中的哪些字面量不可改变”由技术标识保护流程决定，不应写成 GitHub 专用翻译规则。

### 异常优先

模型返回的受保护标记只要缺失、重复或被修改，就不能继续回填损坏的译文。优先跳过该候选并记录可诊断错误，不能猜测仓库名应该插回哪里。

## TODO 1：建立站点内容策略层

- [x] 新增一个小型站点策略模块，输入 `hostname`，输出当前站点的元信息容器和安全排除容器。
- [x] 主机名使用精确匹配；除非有明确需求，不自动扩展到所有子域名。
- [x] 没有站点规则时返回空策略，保持现有网站行为。
- [x] 选择器集中存放，不散落在扫描器、过滤器和渲染器中。
- [x] 规则命名表达页面语义，避免把某次截图中的视觉位置写进代码。

建议接口形状：

```js
getSiteContentPolicy(hostname) => {
  metadataContainers: string[],
  excludedContainers: string[],
}
```

具体选择器必须先用保存的静态 DOM fixture 确认。2026-08-10 已通过 GitHub 个人主页的异步 contributions fragment 确认 `.js-yearly-contributions`、`.graph-before-activity-overview`、`.js-calendar-graph.ContributionCalendar` 和 `.ContributionCalendar-grid` 的边界，并将最小 fixture 固化到集成测试。

## TODO 2：修复 Hacker News 元信息残片

- [x] 为 `news.ycombinator.com` 增加元信息容器规则。
- [x] 用真实条目结构制作最小静态 fixture，覆盖积分、作者、相对时间、`hide` 和评论数。
- [x] 将命中的容器分类为 `metadataOnly`，继续复用 `skipSocialMetadata` 的默认行为。
- [x] 不按 `points`、`by` 或竖线数量做全局文本匹配。
- [x] 保证新闻标题、长链接标题和正文仍进入翻译流程。

验收标准：

- 默认设置下，翻译请求中不存在 `35 points by | |` 或同结构残片。
- 标题仍正常提交并生成译文。
- 相同 DOM 放在其他主机名下时不触发 Hacker News 规则。
- 用户关闭 `skipSocialMetadata` 后，行为仍遵守现有设置契约。

## TODO 3：保护 GitHub contribution calendar 布局

- [x] 为 `github.com` 确认 contribution calendar 的最小稳定根容器。
- [x] 将整个固定布局可视组件加入 `excludedContainers`，不要逐个排除月份、星期或色块。
- [x] 规则范围只覆盖图表及其紧耦合标签，不能吞掉下方 contribution activity 正文。
- [x] 排除规则同时作用于初次扫描、动态内容扫描和渲染前复核。
- [x] 不通过调整 `.bt-translation` 字号、定位或 `z-index` 掩盖问题。

已确认的排除根是 `github.com` 下 `.js-yearly-contributions` 内的 `.graph-before-activity-overview`。它覆盖日历表格、坐标、日格辅助文本和图例，但不排除上方年度汇总，也不排除下方 `#user-activity-overview`。识别逻辑保留在站点策略层，不扩展成全局 table、ARIA grid、月份或星期文本规则。

验收标准：

- 月份、星期、图例、色块辅助文本不产生翻译请求。
- contribution calendar 内不插入 `.bt-translation`，也不写入 `data-bt-source`。
- 图表外的普通简介、活动描述和文章正文仍正常翻译。
- 设置变化或动态重扫不会重新向图表内部插入节点。

## TODO 4：跳过 GitHub 仓库语言占比组件

- [ ] 为 `github.com` 确认 repository language statistics 的最小稳定根容器。
- [ ] 将语言名称、百分比、颜色图例和进度条作为一个不可翻译组件处理，不逐项创建译文节点。
- [ ] 规则按组件上下文生效，不维护全局“所有编程语言”黑名单。
- [ ] 自动覆盖 GitHub 展示的编程语言、标记语言、样式语言、框架、模板语言和未来新增分类。
- [ ] 规则同时覆盖仓库主页和其他复用同一语言占比组件的 GitHub 页面。
- [ ] 保证组件之外的 README、仓库描述、提交信息和普通正文继续翻译。

验收标准：

- `JavaScript`、`CSS`、`Vue`、`HTML` 及其百分比不产生翻译请求。
- `C`、`R`、`Go`、`Zig`、`Gleam` 等已知或新增标签获得相同行为，无需逐项加入词表。
- 组件内不插入 `.bt-translation`，不改变色条、图例和百分比布局。
- 普通正文中的 `Go to settings` 等句子仍正常翻译。
- 相同文本或相同类名出现在其他主机名时不触发 GitHub 规则。

## TODO 5：实现句内技术标识保护

- [ ] 扫描候选时保留文本节点来源，识别嵌在自然语言中的技术标识，而不只判断整个候选文本。
- [ ] 优先利用 DOM 边界保护链接中的仓库名、文件名和路径；普通自然语言链接仍允许翻译。
- [ ] 将受保护字面量替换为本段唯一的稳定占位标记，再把完整上下文发送给模型。
- [ ] 在翻译提示中明确要求逐字保留占位标记。
- [ ] 回填前验证每个标记恰好出现一次、数量一致且顺序可恢复。
- [ ] 验证通过后恢复原始字面量；缓存保存恢复后的最终译文。
- [ ] 验证失败时跳过该候选并记录错误，不显示缺少仓库名的译文。
- [ ] `skipTechnicalIdentifiers` 继续控制“纯技术标识候选是否跳过”；嵌在正文中的标识始终作为不可变字面量保护。

目标结果：

```text
原文：Contributed to yuukiLike/cc-md-vault, yuukiLike/music-eyes,
      yuukiLike/cc-md-preview and 107 other repositories

译文：贡献至 yuukiLike/cc-md-vault、yuukiLike/music-eyes、
      yuukiLike/cc-md-preview 以及另外 107 个仓库
```

允许模型根据目标语言调整连接词和标点，但三个仓库名的内容、数量和顺序必须完全不变。

## TODO 6：避免原文与译文相同时重复渲染

- [x] 在渲染前使用与源文本一致的规范化规则比较原文和译文。
- [x] 规范化后完全相同时，将候选标记为完成但不创建 `.bt-translation`。
- [x] 该规则只作为防御性兜底，不能替代扫描前的组件排除和技术标识保护。
- [x] 保留正常的大小写、标点和语言差异；只有真正等价的不变结果才跳过渲染。

验收标准：

- 模型原样返回 `JavaScript`、`API` 或其他不可译术语时，页面不出现重复文本。
- 原文与译文只有无意义空白差异时不重复渲染。
- 合法译文仍正常回填，不因包含相同技术词而整段跳过。

## TODO 7：测试与回归保护

- [x] 为站点策略模块增加主机名和选择器匹配单元测试。
- [x] 为 Hacker News 增加静态 DOM 集成测试。
- [x] 为 GitHub contribution calendar 增加固定布局组件排除测试。
- [ ] 为 GitHub 仓库语言占比组件增加已知、歧义和未知技术标签测试。
- [ ] 为句内技术标识增加单个、多个、重复、相邻标点和换行场景测试。
- [ ] 增加模型删除、复制或修改占位标记时的失败路径测试。
- [ ] 增加原文与译文相同、仅空白不同和真正译文三类渲染测试。
- [x] 验证动态新增节点继续应用相同规则。
- [x] 验证相同类名和相同文本出现在其他主机名时不被站点规则误伤。
- [x] 运行 `npm run check`，确认生成产物、源码检查和全部测试通过。

测试只使用现有 Node.js、Happy DOM、契约测试和构建检查。除非用户单独授权，不启动 Chrome、Chromium、截图或视觉对比流程。

## 建议实施顺序

1. 先建立站点策略接口和主机名隔离测试。
2. 接入 Hacker News 元信息分类、GitHub contribution calendar 和语言占比组件排除。
3. 再实现句内技术标识占位、验证和恢复。
4. 增加相同译文不重复渲染的防御性兜底。
5. 最后补齐动态内容回归测试并运行完整检查。

前两步控制 DOM 边界，第三步保证提交给模型的数据契约，第四步阻止无变化结果产生重复 DOM。三层问题应分别解决，不能用一个不断扩大的文本正则同时承担页面识别、语言词典和字面量保护。

## 非目标

- 不在本轮重写整个 DOM 扫描器或渲染器。
- 不为每个 GitHub 文案增加翻译词典。
- 不维护容易误伤 `Go`、`R`、`C` 等普通文本的全局编程语言黑名单。
- 不使用模型提示词代替确定性的标识符恢复校验。
- 不全局排除表格、链接、短句、`.subtext` 或 `.score`。
- 不修改 GitHub 或 Hacker News 原有节点、样式和交互行为。
