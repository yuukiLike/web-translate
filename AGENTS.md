# Development Rules

## Code Style

- 可读性优先于代码行数和技巧性。使用清晰、显式的控制流和必要的中间变量，避免嵌套三元表达式、过长的方法链和难以快速理解的单行代码。
- 每个函数只承担一个明确职责。复杂流程拆成具有业务含义的步骤；只有在抽取能表达意图或形成真实复用时才增加抽象，避免无意义的转发函数和过度封装。
- 命名必须直接表达用途和数据含义，避免含糊缩写及 `data`、`item`、`temp` 等缺少上下文的名称。注释用于解释不直观的原因、约束和取舍，不重复描述代码表面行为。

## Design System

- 修改 Chrome 设置页、图标或其他视觉界面前，先阅读 `DESIGN.md`；未经明确确认，不偏离其中的首次配置路径、色彩、间距和组件约束。
